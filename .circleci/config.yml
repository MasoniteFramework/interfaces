version: 2
jobs:
    build:
        docker:
            - image: 'circleci/python:3.6'
        steps:
            - checkout
            - run: pip install .
            - run: pip install pytest
            - run: python -m pytest

workflows:
    version: 2
    builds_and_test:
        jobs:
            - build